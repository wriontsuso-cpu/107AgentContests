# AI 资源导航助手 · 数据补充包（整合去重结果）

> 本 README 记录本次「数据补充与更新」的发布内容、更新时间与数据质量保证。
> 对应数据文件：`原始数据_整合.json`（无 search_text）、`原始数据_整合_search_text.json`（含 search_text）。

---

## 一、本次更新时间与发布摘要

| 项 | 值 |
| --- | --- |
| **数据生成时间** | 2026-08-20T23:11:00 UTC（北京时间 2026-08-21 07:11） |
| **条目总数** | **1653** 条（上一版 1295 条 → 本轮补充 359 条） |
| **覆盖栏目** | **33 个** |
| **数据组成** | `curated` 31 条 + `crawled` 1622 条 |
| **search_text 覆盖率** | 1653 / 1653（100%） |
| **脱敏** | sanitized = true，PII 复扫 0 命中 |
| **规范化 URL 去重** | 重复组数 = 0 |

> 上一版对应快照 1295 条，本轮基于已有公开采集扩展（图书馆电子资源、青春科大扩展、二课/团学活动、教务服务等来源）合并入库，形成 1653 条、覆盖 33 个栏目的完整数据底座。

---

## 二、本次更新内容（相对上一版 1295 条）

1. **数据扩充**：新增 **359 条**窄覆盖资源，主要由以下公开采集来源并入：
   - 图书馆电子资源（新增 233+ 条，字段含 `access_url` / `db_status` / `subscription`）
   - 青春科大扩展、二课/团学活动、教务服务等公开栏目。
2. **去重**：规范化 URL 去重后重复组数 = 0；
   - 清理 6 组乱码副本（同一 URL 下 GBK 误码拷贝与纯净版并存时保留纯净版）；
   - 合并 1 组 http/https + URL 大小写重复；
   - 删除不再属于权威 33 分类的孤儿栏目 `新生事务`（共 4 条，经核对其 URL 全部完整保留在 `迎新资讯` 等正确栏目下，无内容丢失）。
3. **脱敏**：全库复扫 title/summary/content/search_text/how_to/cost/source，
   身份证/手机号/邮箱/学号/QQ/微信/银行卡等真实身份命中 **0** 条；全部替换为 `[敏感内容已脱敏]` 占位。
4. **结构完整**：文档树中按栏目合计 1653 = 主库 1653，完全一致；顶层字段与 kind 分布一致。
5. **数据接口规范.md** 同步更新：条数 1295→1653、组成来源、分类树 32→33、`kind` 字段可空说明。
   接口约定（检索 / 展示 / 联动）与字段结构不变，不影响 B/C/D 三方现有对接，仅数据量增加、分类更准。

---

## 三、数据组成与去重策略

- **组成**：非登录 627 条 + mine 783 条 + 扩展采集（图书馆资源 / 青春科大 / 二课团学活动 / 教务服务等），经 URL 归一化 + 标题指纹内容级去重后共 **1653 条**。
- **去重策略**：先按 URL 归一化去重（统一 http/https、去锚点、去末尾斜杠），再按「标题指纹」做内容级去重——同一讯息来自不同网址时整合为一条，以校级 > 职能部门 > 部门服务 > 学生自建/聚合的官方权威顺序保留最权威来源，其余网址记入「其他来源链接」。短标题（导航/入口类）不参与内容合并。

---

## 四、按栏目分布（33 类）

详见 `文档/按栏目/*.md` 与 `文档/02_全量清单.md`；每栏目标题含各栏目条目数。

---

## 五、数据文件说明

| 文件 | 说明 |
| --- | --- |
| `原始数据_整合.json` | 无 `search_text` 字段，供普通展示/读取 |
| `原始数据_整合_search_text.json` | 含 `search_text` 字段，供语义检索（B）embedding |
| `文档/` | 00 总览 / 01 按栏目索引 / 02 全量清单 / 按栏目/*.md |
| `表格/` | `整合去重_全量.csv` + `.xlsx` |
| `PDF/` | `整合去重_汇总.pdf`（5 页） |
| 根目录 `数据接口规范.md` | 前后端对接唯一权威规范 |

---

*本数据由 crawler 工作区流水线（采集 → 去重 → 脱敏 → search_text → 发布）产出，提交于分支「数据补充与更新」。*
