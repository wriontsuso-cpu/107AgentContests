# 107AgentContests

107 杯智能算力开发赛 —「AI 资源导航助手」团队仓库（数据集 · 接口规范 · 各成员模块）。

## 仓库结构

- `data without log in/`：整合去重后的资源数据集发布镜像（JSON ×2 / 文档树 / 表格 / PDF）
- `数据接口规范.md`：前后端对接唯一权威规范（与 `crawler/` 工作区、`107` 根目录三处同步）
- `member-A/`：成员 A 数据采集流水线（公众号爬取、本地文档入库、团队数据集导出）
- `_master_snapshot.txt` / `_searchtext_stats.txt`：主库统计快照

## 数据现状（2026-08-28 · 数据库整理助手 v2 整理后）

- 条目总数 **12882**（curated 63 + crawled 651 + crawl 12168），栏目 39 个
- 内容级去重：13188 → 12882（标题指纹 149 组 + 内容相似度 157 组自动合并，共 -306）；URL 规范化 0 重复组；年份冲突/近重复 4694 对待复核未合并（保持独立）
- 三性审核：全库打标 `info_status/event_date/expired/freshness` + `disposition`（keep 3849 / flag 6449 / deprecate 2584，只标注不删除）；有效性分层抽样探活 120 条全 reachable
- 脱敏：PII 8 类 dry-run 0 命中、写回复扫 0 命中；线下文档 `file://` 本地绝对路径（含用户名）已中性化脱敏
- search_text 覆盖 12882/12882
- 权威主库位于 crawler 工作区：`crawler\data without log in\原始数据_整合_search_text.json`；本目录为发布镜像，由 crawler 主库重建同步


## 数据现状（2026-08-30 · 内容级信息补足）

- 全库 **12882/12882** 条新增可选字段 `content_info = {"digest": "...", "points": [...]}`，由「数据库内容提炼」子 Agent 分 65 批逐条总结提炼（复用此前 180 条已提炼内容）
- digest：1–2 句中文内容级总结（全库平均约 40 字）；points：每条恰 2 条要点（平均约 11 字）
- 口径约束：无 URL、8 类 PII 扫描 0 命中、无 5 位以上连续数字；未覆盖记录不携带该字段，前端需做缺省处理
- 不破坏接口规范：`数据接口规范.md` 仅新增 `content_info` 字段行与子字段口径说明（148→153 行），原字段、search_text 组合、39 栏目分类树均不变
- 质检：`运行/数据库整理助手_20260829_内容补足_v1/31_内容补足_质检报告.md` 结论「通过」（覆盖 12882/12882、PII 0、master==repo、39 栏目）
- 整理工具迭代：database-organizer skill v1.3.0（新增「多 Agent 执行约定」：并发错峰、429 降级幂等重试、校验脚本验收）
## 数据现状（2026-08-30 · 整理优化轮）

- 条目总数 **12882**、栏目 39 不变；内容级去重幂等复核 0 自动合并（待复核 4407 对保持独立、数据无损）
- 三性审核以 2026-08-30 为基准日重算：disposition keep 3845 / flag 6449 / deprecate 2588（只标注不删除）
- 有效性分层抽样 120 条本轮返回 403/429，按口径判 blocked（登录态/WAF，不判内容错误），全库仅保留本轮标注
- content_info 2 处口径超限修正（digest ≤90 字、points ≤24 字），全库覆盖 12882/12882 合规
- 接口规范补登记：整理助手审核/脱敏标注字段（info_status/event_date/expired/freshness/sanitized 等）与会员 A 可选字段（access_url/db_status/subscription）补入字段字典；`kind` 行冗余括注修复；顶层结构登记 `sanitized`
- PII 8 类 dry-run 与写回复扫 0 命中；发布数据无本地绝对路径/用户名；质检「通过」

## 合规边界

仅收录公开/指导型内容；不含成绩、选课、消费流水、体检、资助申请等个人记录；所有 Cookie 文件不入库；发布数据不含本地绝对路径/用户名。

