# Copy to .env.development.local for local frontend/backend integration.
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_USE_MOCKS=false
# The backend can perform several verification steps before returning an answer.
VITE_ASSISTANT_REQUEST_TIMEOUT_MS=160000
