# 数据接口规范（Data Interface Specification）

> 适用范围：107 杯智能算力开发赛 —「AI 资源导航助手」内部团队（A/B/C/D）
> 负责人：成员 D（集成与文档）牵头，与 A/B/C 对齐
> 最后更新：2026-09-02
> 对应数据文件：`data without log in\原始数据_整合.json`

本规范定义资源数据集的**统一字段**、**读取方式**与**为防 LLM 幻觉而做的 URL 校验约定**，作为前后端对接的唯一权威依据。

---

## 一、数据位置与读取

| 项 | 值 |
| --- | --- |
| 数据文件 | `data without log in\原始数据_整合.json` |
| 顶层结构 | `{ generated_at, total, curated, crawled, search_text_generated, sanitized, source_total, excluded_total, excluded_by_reason, articles[] }` |
| 原始记录 | 12812 |
| 去重后记录 | 12810（合并 2 组重复 URL） |
| 当前发布 | 12382 |
| 发布排除 | 430 条：dead 392、unknown 8、本地文件缺失 24、错误跳转 3、空标题 1、重复记录 2 |

**读取方式**：B 的后端服务读 `articles[]` 数组，每条为一条资源。当前文件为发布数据，保留登录或访问受限但确认有效的链接，排除确认失效及暂时无法确认的链接。数据为纯 JSON（UTF-8），可直接 load 进内存 / 向量库。

---

## 二、字段规范（数据字典）

每条资源记录的字段、类型、含义、来源：

| 字段 | 类型 | 必填 | 含义 | 来源 |
| --- | --- | --- | --- | --- |
| `title` | string | ✅ | 资源/讯息标题 | A 清洗后 |
| `id` | string | （爬虫产物才有） | 由 url 的 md5 前16位生成，稳定标识 | A |
| `url` | string | ✅ | 资源/讯息原始链接（权威链接） | A |
| `source` | string | ✅ | 来源名称（中文域名标签，如“教务处”“图书馆”） | A |
| `category` | string | ✅ | 所属栏目（39 个之一） | A |
| `summary` | string |  | 简介摘要 | A |
| `content` | string |  | 正文（多数入口类为空） | A |
| `crawled_at` | string |  | ISO 时间 | A |
| `tags` | string[] |  | 标签数组 | A |
| `cost` | string |  | 费用说明 | A |
| `how_to` | string |  | 获取方式 | A |
| `weight` | number |  | **检索权重**（0–10）。入口类、官方服务、精选资源更高；新闻/活动报道更低。用于排序加分 | D |
| `relevance_score` | number |  | 与 `weight` 同步的兼容字段（旧前端/后端仍读此列） | A / D |
| `kind` | string |  | 类型：`crawl`/`crawled`/`curated`（可为空/None，表示非爬虫、未标注类型） | A |
| `source_site` | string |  | 权威主域名 | A |
| `related_urls` | string[] |  | 同一条讯息的其它来源链接（内容合并时） | A |
| `source_count` | number |  | 内容合并时该讯息横跨的来源数（≥1） | A（内容去重） |
| `authority_label` | string |  | 权威等级标签（校级官方/职能部门官方等） | A |
| `search_text` | string | ✅ | **面向语义检索的拼接文本**（标题+栏目+标签+正文） | D |

> `search_text` 由脚本 `_生成searchtext.py` 生成，并在 `scripts/apply_resource_weights.py` 中补上同义词别名。B 可直接用于 embedding 与模糊检索。
>
> `weight` / `relevance_score` 由 `scripts/apply_resource_weights.py` 根据栏目先验、是否为校园高频入口、权威等级、来源合并数等计算。查询时：**文本匹配分 + `weight × 8`**。前后端都做模糊匹配（同义词、拼音、1 字编辑距离），因此「座位预约」「图书管」「tushuguan」应能命中对应官方入口。

| `disposition` | string |  | 三性审核处置（keep=保留 / flag=标记过期供过滤 / deprecate=建议下次发布剔除） | 数据库整理助手（审核） |
| `url_status` | string |  | 有效性全量探活结果（reachable/dead/blocked/unknown/unchecked/local） | 数据库整理助手（审核） |
| `url_checked_at` | string |  | 有效性探活日期（2026-08-31 全量） | 数据库整理助手（探活） |
| `url_http` | string |  | 探活返回码：200/403/404/410/412/502；连接异常为 ERR，本地文件为 local，mailto 为空字符串 | 数据库整理助手（探活） |
| `url_err` | string |  | 不可直连原因说明（登录墙需 CAS / 访问受限 403 / 页面失效 404 / 内容已删除 410 / SSL 异常 / 反爬 / 限流）；可直达为空字符串 | 数据库整理助手（探活） |
| `field_issues` | string[] |  | 正确性审核问题清单（字段口径不符时） | 数据库整理助手（审核） |
| `content_info` | object |  | 内容级信息（可选）：`{"digest": "...", "points": [...]}`；由信息总结提炼 Agent 生成，用于检索/展示的内容级补足 | 数据库整理助手（内容补足） |
| `published_at` | string |  | 发布/上线日期（`YYYY-MM-DD` 或带时间；缺失表示未标注） | A |
| `sanitized` | boolean |  | 是否已通过 PII 脱敏并复扫（true） | 数据库整理助手（脱敏） |
| `info_status` | string |  | 时效状态：`有效(近期发布)`/`有效(一年内)`/`较旧(超一年)`/`已过期`/`近期活动/进行中`/`常青内容/通知`/`时效未知(无日期)` | 数据库整理助手（审核） |
| `event_date` | string |  | 检测到的最新事件日期（`YYYY-MM-DD`；无日期为空字符串） | 数据库整理助手（审核） |
| `expired` | boolean |  | `event_date` 是否早于审核基准日 | 数据库整理助手（审核） |
| `freshness` | string |  | 相对审核基准日的时效描述（`即将开始`/`进行中/近期`/`已过期N天`/`发布N天`；无日期为空字符串） | 数据库整理助手（审核） |
| `access_url` | string |  | 数据库类资源访问入口（会员 A 数据，仅部分记录携带） | A（会员A） |
| `db_status` | string |  | 数据库资源状态（如「试用」；仅部分记录携带） | A（会员A） |
| `subscription` | string |  | 订阅/授权说明（仅部分记录携带，可为空字符串） | A（会员A） |


> `content_info` 子字段口径：`digest`（string，1-2 句内容级总结，≤90 字）、`points`（string[]，2 条要点，每条 ≤24 字）。字段为可选：未覆盖记录不携带该字段，前端需做缺省处理。

> 整理助手标注口径：`disposition`/`url_status`/`url_checked_at`/`field_issues` 与 `info_status`/`event_date`/`expired`/`freshness`/`sanitized` 为数据库整理助手每轮重算或复核的标注字段，只标注不删除；`access_url`/`db_status`/`subscription`/`published_at` 为可选字段，未覆盖记录不携带或为缺省值，前端需做缺省处理。

---

## 三、接口约定

面向前端（C）/后端（B）的调用建议：

### 1. 语义检索接口（B 提供，消费 search_text）
建议用途：问答与推荐。请求里带 query 与 top_k，返回命中资源数组。`top_k` 仅表示返回上限，
最大为 5；后端不得用低相关结果补足数量。没有达到高相关度阈值的结果时返回空数组。

```jsonc
// 请求（前端——> 后端 B，用于问答/检索）
POST /api/search
{
  "query": "秋季要不要抢图书馆座位？",
  "top_k": 5,
  "category": null,        // 可选，限定栏目
  "session_id": "abc123"   // 可选，用于多轮上下文
}
```

```jsonc
// 响应
{
  "results": [
    {
      "title": "图书馆座位预约",
      "url": "https://lib.ustc.edu.cn/...",
      "category": "图书馆",
      "summary": "...",
      "authority_label": "部门服务",
      "source_count": 1,
      "related_urls": []
    }
  ],
  "answer": "基于上述资源的简要回答",
  "session_id": "abc123",
  "response_type": "navigation"
}
```

`response_type` 为兼容性新增字段，取 `navigation`（默认，资源问答）或 `conversation`
（纯问候、感谢、告别、助手身份或功能询问）。旧响应缺少该字段时前端按 `navigation` 处理。
`conversation` 返回简短 `answer`，`results` 与 `clarifications` 均为空数组，无需检索或模型调用；
仍返回 `session_id` 并记录本轮会话，不自动关闭会话。混合消息如“你好，请问图书馆怎么预约”
仍按资源问答处理。该分流只作用于 AI 问答，不改变资源大厅的关键词搜索。

为展示长耗时问答的处理进度，后端同时提供 `POST /api/search/stream`。它接收完全相同的请求体，
以 `application/x-ndjson` 逐行返回事件；`progress` 事件只描述当前正在执行的检索、页面访问与
答案核实步骤，不包含模型内部推理，最后用一个 `result` 事件承载与 `/api/search` 相同的完整响应：

```jsonc
{"type":"progress","stage":"page_fetch","message":"当前正在访问‘图书馆座位预约’网页内容"}
{"type":"result","data":{"results":[],"answer":"...","session_id":"abc123","response_type":"navigation"}}
```

前端无法使用流式接口时可回退到 `POST /api/search`，因此原有调用保持兼容。

### 2. 浏览↔问答联动（D 负责）
用户在树状/图状浏览到某节点，一键对该节点资源发起提问 → 调用 `POST /api/search`，并把 `category` 限定为该节点栏目，返回该栏目内的资源。

### 3. 推荐↔跳转
AI 推荐结果中每个条目的 `url` 可直接跳转到资源详情，`category` 用于跳回对应栏目树。

### 4. 可信网络兜底（数据库未形成有效命中时）

资源问答（`response_type: "navigation"`）仍必须先检索本数据集。数据库召回项只是候选：后端须先读取唯一主资源 URL 并核验实时页面内容。
仅当没有达到高阈值的候选，或候选页面内容无法直接回答问题时，后端可以调用具备网页搜索能力的模型接口。
联网结果不写回本地资源库，并须满足以下附加约束：

- 后端只接受配置允许的可信域名，并校验引用 URL 的协议、域名和来源元数据。
- 网络结果使用 `kind: "web"`，`id` 以 `web-` 开头，`source_site` 为引用页面域名。
- `authority_label` 必须为后端生成的可信等级，不得直接采用模型自述。
- 没有可信引用时，`results` 返回空数组，`answer` 为“当前未检索到合适内容。”
- 数据库经实时页面核验形成有效命中时，不得用网络搜索替换数据库资源。

---

## 四、反幻觉机制（重要）

LLM 可能编造不存在的资源 URL。对接方（C/D/B）在任何把 AI 生成的链接展示给用户、或点击跳转前，**必须用本数据集校验链接真实性**：

```python
def is_known_url_candidate(value, articles):
    """校验 AI 给出的链接/资源名是否存在于数据集。"""
    if not value:
        return False
    # 允许直接匹配到已知 URL
    value_norm = (value or "").replace("http://", "https://").lower().rstrip("/")
    known_norm = {(a.get("url") or "").replace("http://", "https://").lower().rstrip("/")
                  for a in articles}
    if value_norm in known_norm:
        return True
    # 名字检索（便于 AI 给出中文名后映射到真实链接）
    for a in articles:
        title = (a.get("title") or "").strip()
        search = (a.get("search_text") or "").strip()
        if title and (title.lower() in value.lower() or value.lower() in title.lower()):
            return True
    return False
```

> 数据库结果应逐条运行 `is_known_url_candidate`。`kind: "web"` 的联网兜底结果则必须通过后端可信域名、
> `source_site` 与 `authority_label` 的联合校验后才可显示为可点击来源。

### 4. 全量探活与不可直连判定（2026-08-31 起全量）
自 2026-08-31 起，有效性校验由抽样升级为全量，每条记录新增 `url_http` 与 `url_err` 字段。2026-09-02 数据更新后，原始记录为 12812 条，合并 2 组重复 URL 后为 12810 条：

- 判定口径：2xx/3xx=reachable；404/410=dead；401/403/429=blocked；跳转落到 CAS/统一身份认证=blocked（登录墙）；超时/SSL/连接失败=unknown；file:// 本地文档=local；mailto=unchecked。
- i.ustc.edu.cn 为 SPA 站点：详情路由必须带 `/page/site/` 前缀，nginx 直接返回 404 是路由未注册，**不等于**内网登录墙；登录墙的真实表现是 403 / 302 跳 CAS / 200+`{"m":"请登录"}`。
- 前端展示或跳转前，可按 `url_status`/`url_err` 对链接加注（如“需登录”“已下线”），不再直接推送失效链接。

> 2026-09-02 更新结果：在上一轮全量结果基础上移除 70 条无法登录的受限记录；原始数据状态为 reachable 12219 / blocked 168 / dead 392 / unknown 8 / local 24 / unchecked 1。发布数据继续保留确认有效的 blocked 记录，并按上表口径排除 430 条记录，最终为 12382 条。


---

## 五、分类树（39 个栏目）

数据按 `category` 分层，前端可直接作为树的第一/第二层：

- 学术科研类：会议/学术交流、学术科研、校园资讯、校级通知、媒体关注、公示公告
- 教学教务类：教务通知、教务选课、教务服务、研究生培养、本科招生、留学/出境交流、留学/国际交流、新生指南
- 学生发展类：就业实习、二课/团学活动、二课/团学办事指南、社团/文体活动、青春科大、院系一线、学研两会/学生组织、奖助学金、勤工助学、学工通知、竞赛/科创、校园活动
- 校园服务类：图书馆、图书馆资源、生活服务、校医院、财务服务、保卫服务、超算中心、办事指南、资源导航、免费软件/会员、网站入口、中心动态

> 说明：个别栏目名本身含“/”（如“免费软件/会员”“竞赛/科创”“二课/团学活动”），以 JSON 中 `category` 字段原值（39 种）为准，前端按原值分组即可。

## 六、维护与扩展

- 字段变更需在本文档登记，并同步给 A/B/C。
- 新增资源由 A 入库。
- search_text 为派生字段，A/B 修改源字段后由该记录自动重算（或重跑 `_生成searchtext.py`）。

*本文档与数据一并提交比赛作品使用。*
